import { MfHoverDirective } from './mf-hover.directive';

describe('MfHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new MfHoverDirective();
    expect(directive).toBeTruthy();
  });
});
