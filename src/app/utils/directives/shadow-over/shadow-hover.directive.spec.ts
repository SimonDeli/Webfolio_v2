import { ShadowHoverDirective } from './shadow-hover.directive';

describe('ShadowHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new ShadowHoverDirective();
    expect(directive).toBeTruthy();
  });
});
